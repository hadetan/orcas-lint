/** Default interprocedural depth: how many calls a value is followed before bailing. */
export const DEFAULT_TRACE_DEPTH = 2;

/** Default global wall-clock budget for a single run, in milliseconds. */
export const DEFAULT_MAX_TIME_MS = 120_000;

/** Default per-file analysis timeout, in milliseconds. */
export const DEFAULT_PER_FILE_TIMEOUT_MS = 5_000;
